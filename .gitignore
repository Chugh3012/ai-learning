# Secrets & local state — never commit
.env

# Local Python venv & scratch
.venv/
*.tmp
__pycache__/
*.pyc

# FreshRSS persistent data (system of record)
data/freshrss/

# Owned knowledge base (P3) and generated digests (P2/P4)
data/kb/
digests/

# OS / editor noise
.DS_Store
Thumbs.db
