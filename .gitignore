# Secrets & local state — never commit
.env

# Local Python venv & scratch
.venv/
*.tmp
__pycache__/
*.pyc

# FreshRSS persistent data (system of record)
data/freshrss/

# Owned knowledge base (P3) and generated outputs (P2/P4/P5)
data/kb/
digests/
drafts/*.md

# Regenerable eval/training exports (golden set is kept; these are derived on demand)
.foundry/datasets/feedback_*.jsonl
.foundry/results/gate_latest.json

# gh-aw local audit logs (downloaded on demand via `gh aw audit`)
.github/aw/logs/

# OS / editor noise
.DS_Store
Thumbs.db
