# Secrets & local state — never commit
.env

# Local Python venv & scratch
.venv/
*.tmp
__pycache__/
*.pyc

# Owned knowledge base (P3) and local delivery scratch
data/kb/
.scratch/

# Regenerable eval/training exports (golden set is kept; these are derived on demand)
.foundry/datasets/feedback_*.jsonl
.foundry/results/gate_*.json

# gh-aw local audit logs (downloaded on demand via `gh aw audit`)
.github/aw/logs/

# OS / editor noise
.DS_Store
Thumbs.db

# generated multi-topic site (built from topic packs by web/build.py)
web/public/
