# ai-scout — discovery inbox (the "Jarvis" self-growth seam).
#
# prism/services/discoverer.py (sync --discover) WRITES proposed sources here: external domains
# that KB item links keep pointing to but we don't yet ingest, with an auto-discovered RSS/Atom feed.
# Nothing here is live. You review, then approved entries are MERGED into sources.opml and REMOVED
# from here (revise, don't append).
#
# entry shape:
#   - name: <label>
#     candidate_url: <feed or site>
#     kind: native
#     seen_count: <how often the system encountered it>
#     reason: <why proposed>
#     status: proposed   # proposed -> approved -> merged (then delete) | rejected

proposals:
  - name: theregister.com
    candidate_url: https://theregister.com/feed
    kind: native
    seen_count: 3
    reason: linked by 3 KB items; feed auto-discovered
    status: proposed
