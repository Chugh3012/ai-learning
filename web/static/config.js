// Runtime config for the static site. Swap this single file per environment (staging,
// preview, custom domain) without touching main.js. Loaded before main.js.
window.APP_CONFIG = {
  apiBase: "https://fn-ai-scout-fb.azurewebsites.net/api",
};
